## next-gen-sales-service

### Lokalne uruchomienie

docker compose up -d 

docker compose down -v

mvn spring:boot run

Swagger: http://localhost:8080/swagger-ui/index.html#/

Dane testowe są w: /db/schema.sql